<%= form_for(@collection) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :name, placeholder: "New collection name..." %>
  </div>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>